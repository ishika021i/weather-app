name: Print Node Info

on:
  push:
    branches: [develop]

jobs:
  node-info: 
    runs-on : ubuntu-latest

    steps:

     - name: Checkout Repository code 
       uses: actions/chechout@v3

     - name: Print Node.js Version 
       uses: node -v || echo " Node is not installed yet "
       
     - name: List Project File
       uses: ls - la
      